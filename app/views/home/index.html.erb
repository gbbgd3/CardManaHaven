<div class="flex justify-center">
    <h2 class="py-5 text-4xl font-bold text-black align-center flex border-b-8 border-green-200">Products on sale</h2>
</div>
<h3 class="text-2x text-black border-b">Yugioh Cards<small class="ms-2 font-semibold text-blue-500">View all</small></h3>
<div class="flex flex-col">
    <ul class="flex flex-row items-center flex-wrap">
        <% @products_on_sale.each do |p| %>
        <li class="w-1/3">
            <%= render partial: 'layouts/common/yugioh/card', locals:{product: p} %>
        </li>
        <% end %>
    </ul>
    </div>
</div>
